import Header from '../@common/Header';
import { St } from './style';

const MainPage = () => {
  return (
    <St.MainWrapper>
      <Header />
    </St.MainWrapper>
  );
};

export default MainPage;
